// Funções de operações usando Arrow Functions e exportando como módulo
export const add = (a, b) => a + b;
export const subtract = (a, b) => a - b;
export const multiply = (a, b) => a * b;
export const divide = (a, b) => b !== 0 ? a / b : 'Divisão por zero';
